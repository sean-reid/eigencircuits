# In production the worker serves the API at the root (/archive, /abs, ...) and
# the TeX mirror at /texlive/, all same-origin. No /api proxy prefix (that is a
# dev-only vite rewrite), so the client talks to the root.
VITE_API_BASE=
